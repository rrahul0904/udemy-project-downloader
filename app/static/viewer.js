const params = new URLSearchParams(window.location.search);
const lessonId = params.get('lesson');
const requestedTime = Number(params.get('t') || 0);
const PREF_KEY = 'course-intelligence-preferences';
const state = { lesson:null, transcript:null, activeIndex:-1, progress:null, bookmarks:[], lastProgressSave:0 };
const titleEl=document.querySelector('#lesson-title'); const metaEl=document.querySelector('#lesson-meta'); const errorEl=document.querySelector('#viewer-error'); const videoEl=document.querySelector('#media-player'); const audioEl=document.querySelector('#audio-player'); const noMediaEl=document.querySelector('#no-media'); const sourceEl=document.querySelector('#transcript-source'); const searchEl=document.querySelector('#transcript-search'); const countEl=document.querySelector('#search-count'); const transcriptEl=document.querySelector('#transcript'); const autoScrollEl=document.querySelector('#auto-scroll'); const showTimestampsEl=document.querySelector('#show-timestamps'); const progressStatusEl=document.querySelector('#progress-status'); const completeEl=document.querySelector('#mark-complete');
function prefs(){try{return JSON.parse(localStorage.getItem(PREF_KEY)||'{}')}catch(_){return{}}}
function encodedFilePath(path){return String(path).split('/').map(encodeURIComponent).join('/');}
function escapeHtml(value){return String(value??'').replace(/[&<>'"]/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[char]);}
function formatTime(ms){if(ms==null)return'—';const total=Math.max(0,Math.floor(Number(ms)/1000));const h=Math.floor(total/3600),m=Math.floor((total%3600)/60),s=total%60;return h?`${h}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`:`${m}:${String(s).padStart(2,'0')}`;}
async function api(url,options={}){const response=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});if(!response.ok){let message=`Request failed (${response.status})`;try{const payload=await response.json();if(payload?.detail)message=typeof payload.detail==='string'?payload.detail:JSON.stringify(payload.detail);}catch(_){}throw new Error(message);}return response.status===204?null:response.json();}
function currentPlayer(){if(!videoEl.hidden)return videoEl;if(!audioEl.hidden)return audioEl;return null;}
function sourceLabel(item){const language=item.language&&item.language!=='und'?item.language:'unknown';const kind={manual:'Manual',auto:'YouTube Auto',asr:'ASR',translated:'Translation',imported:'Imported'}[item.source_kind]||item.source_kind||'Imported';return `${language} — ${kind} · v${item.version||1}`;}
function configureMedia(){const path=state.lesson?.media_path;videoEl.hidden=true;audioEl.hidden=true;noMediaEl.hidden=true;if(!path){noMediaEl.hidden=false;return;}const src=`/files/${encodedFilePath(path)}`;const player=/\.(mp3|m4a|wav|ogg|flac)$/i.test(path)?audioEl:videoEl;player.src=src;player.hidden=false;player.addEventListener('loadedmetadata',restorePosition,{once:true});player.addEventListener('timeupdate',onTimeUpdate);player.addEventListener('pause',()=>saveProgress(false).catch(showError));player.addEventListener('ended',()=>saveProgress(true).catch(showError));}
function restorePosition(){const player=currentPlayer();const requested=Number.isFinite(requestedTime)&&requestedTime>0?requestedTime:Number(state.progress?.last_position_ms||0);const position=requested/1000;if(player&&Number.isFinite(position)&&position>=0&&position<player.duration)player.currentTime=position;else if(!player&&requested>0)activateSegment(findActiveSegment(requested),true);}
async function loadTranscript(transcriptId){state.transcript=await api(`/api/v1/transcripts/${encodeURIComponent(transcriptId)}`);state.activeIndex=-1;renderTranscript();updateExportLinks();if(requestedTime>0)activateSegment(findActiveSegment(requestedTime),true);onTimeUpdate();}
function bookmarkFor(index){return state.bookmarks.find((item)=>Number(item.segment_index)===Number(index));}
function renderTranscript(){const query=searchEl.value.trim().toLowerCase();let matches=0;const segments=state.transcript?.segments||[];transcriptEl.innerHTML=segments.map((segment,index)=>{const text=String(segment.text||'');const match=query&&text.toLowerCase().includes(query);if(match)matches++;const segmentIndex=Number(segment.segment_index??index);const saved=Boolean(bookmarkFor(segmentIndex));return `<div class="segment ${match?'match':''}" data-index="${index}" data-segment-index="${segmentIndex}" data-start="${segment.start_ms??''}" role="group"><button class="segment-time" type="button" data-seek="${index}" aria-label="Seek to ${formatTime(segment.start_ms)}">${formatTime(segment.start_ms)}</button><button class="segment-text" type="button" data-seek="${index}">${escapeHtml(text)}</button><button class="segment-bookmark ${saved?'saved':''}" type="button" data-bookmark="${index}" aria-label="${saved?'Remove bookmark':'Bookmark this source moment'}">${saved?'★':'☆'}</button></div>`;}).join('')||'<p class="empty">No readable transcript segments.</p>';transcriptEl.classList.toggle('hide-timestamps',!showTimestampsEl.checked);countEl.textContent=query?`${matches} match${matches===1?'':'es'}`:`${segments.length} segments`;transcriptEl.querySelectorAll('[data-seek]').forEach((button)=>button.addEventListener('click',()=>seekToSegment(Number(button.dataset.seek))));transcriptEl.querySelectorAll('[data-bookmark]').forEach((button)=>button.addEventListener('click',(event)=>{event.stopPropagation();toggleBookmark(Number(button.dataset.bookmark)).catch(showError);}));}
function seekToSegment(index){const segment=state.transcript?.segments?.[index];if(!segment)return;const player=currentPlayer();if(player&&segment.start_ms!=null){player.currentTime=Number(segment.start_ms)/1000;player.play().catch(()=>{});}activateSegment(index,true);history.replaceState(null,'',`/viewer?lesson=${encodeURIComponent(lessonId)}&t=${Number(segment.start_ms||0)}`);}
function findActiveSegment(positionMs){const segments=state.transcript?.segments||[];let low=0,high=segments.length-1,candidate=-1;while(low<=high){const mid=Math.floor((low+high)/2);const start=segments[mid].start_ms;if(start==null||Number(start)>positionMs)high=mid-1;else{candidate=mid;low=mid+1;}}if(candidate<0)return-1;const end=segments[candidate].end_ms;if(end!=null&&positionMs>Number(end)&&candidate+1<segments.length)return candidate+1;return candidate;}
function activateSegment(index,forceScroll=false){if(index<0||index===state.activeIndex)return;const previous=transcriptEl.querySelector('.segment.active');if(previous){previous.classList.remove('active');previous.removeAttribute('aria-current');}const next=transcriptEl.querySelector(`.segment[data-index="${index}"]`);if(!next)return;next.classList.add('active');next.setAttribute('aria-current','true');state.activeIndex=index;if(forceScroll||autoScrollEl.checked)next.scrollIntoView({block:'center',behavior:forceScroll?'smooth':'auto'});}
function onTimeUpdate(){const player=currentPlayer();if(!player||!state.transcript)return;const positionMs=Math.floor(player.currentTime*1000);activateSegment(findActiveSegment(positionMs));const now=Date.now();if(now-state.lastProgressSave>=5000){state.lastProgressSave=now;saveProgress(false).catch(showError);}}
async function saveProgress(completed){const player=currentPlayer();const lastPosition=player?Math.floor(player.currentTime*1000):Number(state.progress?.last_position_ms||requestedTime||0);const payload=await api(`/api/v1/lessons/${encodeURIComponent(lessonId)}/progress`,{method:'PUT',body:JSON.stringify({last_position_ms:lastPosition,completed:Boolean(completed||state.progress?.completed)})});state.progress=payload;completeEl.textContent=payload.completed?'Completed ✓':'Mark lesson complete';progressStatusEl.textContent=`Saved at ${formatTime(payload.last_position_ms)}`;setTimeout(()=>{if(progressStatusEl.textContent.startsWith('Saved'))progressStatusEl.textContent='';},1500);}
async function toggleBookmark(index){const segment=state.transcript?.segments?.[index];if(!segment)return;const segmentIndex=Number(segment.segment_index??index);const existing=bookmarkFor(segmentIndex);if(existing)await api(`/api/learning/bookmarks/${encodeURIComponent(existing.id)}`,{method:'DELETE'});else await api(`/api/learning/lessons/${encodeURIComponent(lessonId)}/bookmarks`,{method:'POST',body:JSON.stringify({segment_index:segmentIndex,start_ms:segment.start_ms,end_ms:segment.end_ms,text:segment.text})});const payload=await api(`/api/learning/lessons/${encodeURIComponent(lessonId)}/bookmarks`);state.bookmarks=payload.bookmarks||[];renderTranscript();activateSegment(index);}
function updateExportLinks(){const id=encodeURIComponent(state.transcript.id);for(const format of ['txt','json','srt','vtt']){const link=document.querySelector(`#export-${format}`);link.href=`/api/v1/transcripts/${id}/export?format=${format}`;const disabled=(format==='srt'||format==='vtt')&&!Boolean(state.transcript.has_timestamps);link.classList.toggle('disabled',disabled);link.setAttribute('aria-disabled',String(disabled));if(disabled)link.removeAttribute('href');}}
function keyboard(event){if(event.target.matches('input,textarea,select'))return;const player=currentPlayer();if(event.key==='/' ){event.preventDefault();searchEl.focus();return;}if(!player)return;if(event.code==='Space'){event.preventDefault();player.paused?player.play().catch(()=>{}):player.pause();}else if(event.key.toLowerCase()==='j')player.currentTime=Math.max(0,player.currentTime-10);else if(event.key.toLowerCase()==='k')player.currentTime=Math.min(player.duration||Infinity,player.currentTime+10);}
async function init(){if(!lessonId)throw new Error('Missing lesson id. Open the viewer from Course Intelligence.');const p=prefs();autoScrollEl.checked=p.autoScroll!==false;showTimestampsEl.checked=p.showTimestamps!==false;const [lesson,progress,bookmarkPayload]=await Promise.all([api(`/api/v1/lessons/${encodeURIComponent(lessonId)}`),api(`/api/v1/lessons/${encodeURIComponent(lessonId)}/progress`),api(`/api/learning/lessons/${encodeURIComponent(lessonId)}/bookmarks`).catch(()=>({bookmarks:[]}))]);state.lesson=lesson;state.progress=progress;state.bookmarks=bookmarkPayload.bookmarks||[];titleEl.textContent=lesson.title;metaEl.textContent=[lesson.source_platform,lesson.uploader,lesson.duration?`${Math.round(lesson.duration/60)} min`:null,lesson.media_path?'media available':'transcript-only'].filter(Boolean).join(' · ');completeEl.textContent=progress.completed?'Completed ✓':'Mark lesson complete';const variants=lesson.transcripts||[];if(!variants.length)throw new Error('This lesson has no normalized transcript variants.');sourceEl.innerHTML=variants.map((item)=>`<option value="${escapeHtml(item.id)}">${escapeHtml(sourceLabel(item))}</option>`).join('');configureMedia();await loadTranscript(variants[0].id);}
function showError(error){console.error(error);errorEl.textContent=error?.message||String(error);errorEl.hidden=false;}
sourceEl.addEventListener('change',()=>loadTranscript(sourceEl.value).catch(showError));searchEl.addEventListener('input',renderTranscript);showTimestampsEl.addEventListener('change',()=>transcriptEl.classList.toggle('hide-timestamps',!showTimestampsEl.checked));completeEl.addEventListener('click',()=>saveProgress(true).catch(showError));document.addEventListener('keydown',keyboard);
window.addEventListener('pagehide',()=>{const player=currentPlayer();if(!lessonId||!player)return;fetch(`/api/v1/lessons/${encodeURIComponent(lessonId)}/progress`,{method:'PUT',keepalive:true,headers:{'Content-Type':'application/json'},body:JSON.stringify({last_position_ms:Math.floor(player.currentTime*1000),completed:Boolean(state.progress?.completed)})}).catch(()=>{});});
init().catch(showError);
