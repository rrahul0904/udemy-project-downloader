const PREF_KEY='course-intelligence-preferences';
function prefs(){try{return JSON.parse(localStorage.getItem(PREF_KEY)||'{}')}catch(_){return {}}}
function init(){const form=document.querySelector('#job-form');if(!form)return;const p=prefs();if(p.quality&&form.elements.quality)form.elements.quality.value=p.quality;if(p.subtitles&&form.elements.subtitle_languages)form.elements.subtitle_languages.value=p.subtitles;document.querySelector('#validate-url')?.addEventListener('click',()=>{const raw=String(form.elements.course_url?.value||'').trim();const status=document.querySelector('#form-message');if(!raw){status.textContent='Enter a Udemy course, YouTube video, or YouTube playlist URL.';return;}try{const url=new URL(raw);const host=url.hostname.toLowerCase().replace(/^www\./,'');const udemy=host==='udemy.com'||host.endsWith('.udemy.com');const youtube=host==='youtube.com'||host==='m.youtube.com'||host==='youtu.be';const isChannel=youtube&&(/\/(@|channel\/|c\/|user\/)/.test(url.pathname));if(udemy&&url.pathname.includes('/course/'))status.textContent='Supported Udemy course URL. Authentication is still validated when the archive job starts.';else if(youtube&&!isChannel&&(host==='youtu.be'||url.pathname==='/watch'||url.pathname.startsWith('/playlist')))status.textContent='Supported YouTube video/playlist URL. Final authorization and availability are checked when the job starts.';else if(isChannel)status.textContent='Channel-wide URLs are intentionally unsupported. Use a specific video or playlist.';else status.textContent='This URL is not in the supported archive surface.';}catch(_){status.textContent='Enter a valid HTTPS URL.';}});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
